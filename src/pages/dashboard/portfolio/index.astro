---
export const prerender = false;

import DashboardLayout from '../../../layouts/Dashboard.astro';
import NexusDashboard from '../../../components/NexusDashboard';
import { getNexusLocals } from '../../../lib/nexus/supabase';

const { user } = getNexusLocals(Astro.locals);
---
<DashboardLayout title="Nexus Data">
  <div class="portfolio-header">
    <h1>Nexus Data</h1>
    <p class="portfolio-user">Logged in as <strong>{user.email}</strong></p>
  </div>

  <NexusDashboard client:load userId={user.id} userEmail={user.email} />
</DashboardLayout>

<style>
  .portfolio-header {
    margin-bottom: 2rem;
  }

  h1 {
    margin-bottom: 0.25rem;
  }

  .portfolio-user {
    color: var(--az-text-muted);
    font-size: 0.9rem;
  }

  .portfolio-user strong {
    color: var(--az-text);
  }
</style>
// force rebuild Tue Mar 24 07:37:16 PM -04 2026
